@model AimlBotWeb.Features.Categories.CategoryModel

@{
    ViewBag.Title = "Details";
}

<div>
    <h4>Категория</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ImageUrl)
        </dt>
        <dd>
            <img src="@Model.ImageUrl">
        </dd>

    </dl>
</div>
<p>
    <span style="display:inline-block"><a class="btn btn-default" href="@Url.Action("Update")">Изменить</a></span>
    <span style="display:inline-block"><a class="btn btn-default" href="@Url.Action("All")">Назад к списку</a></span>
</p>
