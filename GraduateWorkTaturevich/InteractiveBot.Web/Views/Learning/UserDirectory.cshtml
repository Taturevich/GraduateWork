@model IEnumerable<AimlBotWeb.Features.Learning.BotPatternsModel>

@{
    ViewBag.Title = "UserDirectory";
}

<h2>Словарь бота</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BotPattern)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BotTemplate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BotPattern)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BotTemplate)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>*@
        </tr>
    }

</table>
